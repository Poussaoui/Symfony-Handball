{{ form_start(form) }}
    <table class="table">
        <tbody>
            <tr>
                <th>Nom équipe</th>
                <td>{{ form_widget(form.libelle) }}</td>
            </tr>
            <tr>
                <th>Entraineur</th>
                <td>{{ form_widget(form.entraineur) }}</td>
            </tr>
            <tr>
                <th>Creneaux</th>
                <td>{{ form_widget(form.creneaux) }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ form_widget(form.commentaire) }}</td>
            </tr>
            <tr>
                <th>Url calendrier</th>
                <td>{{ form_widget(form.url_result_calendrier) }}</td>
            </tr>
            <tr>
                <th>Url images</th>
                <td>{{ form_widget(form.url_photo) }}</td>
            </tr>
        </tbody>
    </table>
    <button class="btn">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
